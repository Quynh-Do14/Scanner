import {LAYOUT_MARGIN} from '@/constants';
import {makeStyles} from '@/utils';

const changePasswordScreenStyles = makeStyles((theme) => ({
  container: {
    margin: LAYOUT_MARGIN,
  },
}));

export default changePasswordScreenStyles;
